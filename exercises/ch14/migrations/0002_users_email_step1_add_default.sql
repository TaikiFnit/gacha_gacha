-- ============================================================================
-- 0002 — users に email 列を default 付きで追加 (段階デプロイ Step 1/3)。
-- ============================================================================
-- いきなり NOT NULL を即適用するのではなく、 まずは「全行に空文字を埋める」 状態で
-- 列だけ追加する。 アプリ側でこの列を読み書きするコードはまだ無くてもよい。
-- ============================================================================

ALTER TABLE users
    ADD COLUMN email TEXT NOT NULL DEFAULT '';
