-- ============================================================================
-- ch14 / step 2 — 最初のマイグレーション (0001_initial.sql) について。
-- ============================================================================
-- 中身そのものは exercises/ch14/migrations/0001_initial.sql に置いてある。
-- ここはコメントだけ。 ランナーは migrations/ ディレクトリを見るので、
-- このファイル自体は流さなくて良い。
-- ============================================================================

-- 設計のコツ:
--   * 「最初の状態」 を 1 ファイルにまとめる。 後の 0002, 0003 ... は差分だけ。
--   * IF NOT EXISTS / ON CONFLICT DO NOTHING を活用すると、
--     学習中に何度同じ DB に流してもエラーにならない。
--   * 本番では IF NOT EXISTS は使わず厳格に書くのが筋 (二重適用は schema_migrations が防ぐ)。
--   * 1 ファイルが 100 行を超えたら、 機能ごとに分けるサインかも。

-- 流し方は step3 のランナーに任せる:
--   python exercises/ch14/step3_runner.py exercises/ch14/migrations
