-- ============================================================================
-- ch13 / step 4 — users.public_box 列を追加 (本人だけが切替可能なフラグ)。
-- ============================================================================

ALTER TABLE users
    ADD COLUMN IF NOT EXISTS public_box BOOLEAN NOT NULL DEFAULT FALSE;

-- ----------------------------------------------------------------------------
-- 想定される操作
-- ----------------------------------------------------------------------------
-- 自分の Box を公開する:
--   UPDATE users SET public_box = TRUE WHERE id = :me;
--
-- 公開しているユーザーの一覧 (ランキング画面など):
--   SELECT id, name FROM users WHERE public_box = TRUE;
--
-- ----------------------------------------------------------------------------
-- 演習
-- ----------------------------------------------------------------------------
-- (a) 「フレンドにだけ Box を見せる」 という設定値が欲しいとき、
--     どう拡張する? (ヒント: TEXT で 'private'|'friends'|'public' の 3 値)
--
-- (b) DEFAULT FALSE のまま既存ユーザーは全員非公開で開始するのが安全。
--     これを TRUE に変えるなら<strong>本人の同意フローが必要</strong>。
